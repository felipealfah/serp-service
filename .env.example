# Copie para .env (que fica fora do git) e ajuste:
#   cp .env.example .env

# Chave exigida no header X-API-Key. Gere uma forte, ex: openssl rand -hex 24
API_KEY=troque-por-uma-chave-secreta

# Buscas simultâneas — cuidado com RAM do NAS e com /sorry do Google
MAX_CONCURRENCY=2
